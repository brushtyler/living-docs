{
  "name": "living-docs",
  "description": "Automates documentation updates, synchronization, and UI snapshots. Use this whenever you need to update or check documentation.",
  "version": "1.1.0",
  "actions": [
    {
      "name": "check_staleness",
      "description": "Identifies which documentation files are out of sync with recent code changes.",
      "steps": [
        {
          "command": "python3 scripts/git_helper.py staleness",
          "description": "Checking for stale documentation."
        }
      ]
    },
    {
      "name": "take_snapshot",
      "description": "Capture a screenshot of a URL or a specific UI component using a JSON task list.",
      "parameters": [
        {
          "name": "tasks",
          "description": "Path to the JSON file containing the browser actions.",
          "required": true
        },
        {
          "name": "output-metadata",
          "description": "Optional path to save extracted element information.",
          "required": false
        }
      ],
      "steps": [
        {
          "command": "bash scripts/run_bot.sh --tasks {{tasks}} {{#if output-metadata}}--output-metadata {{output-metadata}}{{/if}}",
          "description": "Executing the web snapshot bot."
        }
      ]
    },
    {
      "name": "sync_ui_docs",
      "description": "Scans the project for 'snapshot-recipe' blocks in Markdown files and updates all screenshots from the live site.",
      "parameters": [
        {
          "name": "only_images",
          "description": "Comma-separated list of image filenames or paths to update.",
          "required": false
        },
        {
          "name": "only_files",
          "description": "Comma-separated list of Markdown files to scan.",
          "required": false
        }
      ],
      "steps": [
        {
          "command": "python3 scripts/updater.py {{#if only_images}}--only-images {{only_images}}{{/if}} {{#if only_files}}--only-files {{only_files}}{{/if}}",
          "description": "Synchronizing documentation visuals."
        }
      ]
    },
    {
      "name": "regen_all",
      "description": "Runs the full documentation regeneration pipeline: checks for staleness and updates all UI screenshots.",
      "parameters": [
        {
          "name": "force",
          "description": "Force a visual sync even if no recipe changes are detected.",
          "type": "boolean",
          "default": false
        },
        {
          "name": "only_images",
          "description": "Comma-separated list of image filenames or paths to update.",
          "required": false
        },
        {
          "name": "only_files",
          "description": "Comma-separated list of Markdown files to scan.",
          "required": false
        }
      ],
      "steps": [
        {
          "command": "python3 scripts/orchestrator.py {{#if force}}--force-sync{{/if}} {{#if only_images}}--only-images {{only_images}}{{/if}} {{#if only_files}}--only-files {{only_files}}{{/if}}",
          "description": "Running the master regeneration orchestrator."
        }
      ]
    },
    {
      "name": "resolve_route",
      "description": "Finds the web route (URL) associated with a specific UI component source file.",
      "parameters": [
        {
          "name": "file",
          "description": "The path to the UI component file (e.g., .tsx, .jsx).",
          "required": true
        }
      ],
      "steps": [
        {
          "command": "python3 scripts/resolver.py {{file}}",
          "description": "Resolving UI route for component."
        }
      ]
    }
  ]
}
